version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: budgetplanner_backend
    hostname: budgetplanner
    ports:
      - "8010:8000"
    volumes:
      - ./backend:/app
    environment:
      - ENV=development